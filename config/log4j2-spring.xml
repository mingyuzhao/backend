<?xml version="1.0" encoding="UTF-8"?>
<Configuration>
	<Appenders>
		<Console name="Console" target="SYSTEM_OUT">
			<PatternLayout
				pattern="%style{%d{ISO8601}}{black} %highlight{%-5level }[%style{%t}{bright,blue}] %style{%C{1.}}{bright,yellow}: %msg%n%throwable" />
		</Console>

		<RollingFile name="RollingFile"
			fileName="./logs/tectris-api.log"
			filePattern="./logs/tectris-api-%d{-dd-MMMM-yyyy}-%i.log">
			<PatternLayout>
				<pattern>%d %p %C{1.} [%t] %m%n</pattern>
			</PatternLayout>
			<Policies>
				<!-- rollover on startup, daily and when the file reaches 10 MegaBytes -->
				<OnStartupTriggeringPolicy />
				<SizeBasedTriggeringPolicy size="10 MB" />
				<TimeBasedTriggeringPolicy />
			</Policies>
			<DefaultRolloverStrategy max="10">
				<Delete basePath="./logs/" maxDepth="1">
					<IfAll>
						<!-- Looks for file names starting with partsmaster-service*.log*. Any file including 
							partsmaster-service.log.1 will satisfy this condition. Could delete current log partsmaster-service.log -->
						<IfFileName glob="tectris-api*.log*" />
						<!-- Looks for files that are older than 2 mins -->
						<IfLastModified age="2m" />
						<!-- This means there need to be 11 fails that satisfy the above conditions, 
							that i.e. their name is partsmaster-service*log* and have time stamp greater than 2 mins. 
							Keeps the most recent files that satisfy the condition -->
						<IfAccumulatedFileCount exceeds="11" />
					</IfAll>
				</Delete>
			</DefaultRolloverStrategy>
		</RollingFile>
		
	</Appenders>

	<Loggers>
		<!-- LOG everything at INFO level -->
		<Root level="info">
			<AppenderRef ref="Console" />
			<AppenderRef ref="RollingFile" />
		</Root>

		<!-- LOG "com.toyota.tmmc.cpcpicklist*" at TRACE level -->
		<Logger name="com.toyota.tmmc.cmsapi" level="trace"></Logger>
	</Loggers>

</Configuration>